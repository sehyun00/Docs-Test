# Antigravity Skills 시스템 가이드

> Antigravity AI 에이전트의 확장 기능인 Skills에 대한 개요 및 사용 방법

## 개요

Antigravity는 `.agent` 폴더를 통해 AI 에이전트의 기능을 확장합니다. 이 폴더에는 두 가지 핵심 확장 시스템이 있습니다:

| 구분 | Workflows | Skills |
|------|-----------|--------|
| **위치** | `.agent/workflows/` | `.agent/skills/` |
| **용도** | 반복 작업 자동화 (커밋, 빌드 등) | 지식/가이드라인 참조 |
| **트리거** | `/slash-command` 형태로 명시적 호출 | 작업 컨텍스트에 따라 자동 또는 수동 참조 |
| **파일 형식** | 단일 `.md` 파일 + 프론트매터 | `SKILL.md` + 추가 리소스 폴더 |
| **예시** | `/commit`, `/prd-prepare` | React 베스트 프랙티스, 코딩 스타일 가이드 |

---

## 폴더 구조

```
.agent/
├── workflows/              # 🔄 워크플로우 (슬래시 명령어)
│   ├── commit.md           # /commit 명령
│   ├── prd-prepare.md      # /prd-prepare 명령
│   └── ...
│
└── skills/                 # 🧠 스킬 (지식/가이드라인)
    └── {skill-name}/       # 스킬 폴더
        ├── SKILL.md        # 필수: 스킬 진입점
        ├── scripts/        # 선택: 헬퍼 스크립트
        ├── examples/       # 선택: 예제 코드
        └── resources/      # 선택: 추가 리소스
```

---

## SKILL.md 파일 형식

스킬의 핵심은 `SKILL.md` 파일입니다. YAML frontmatter와 마크다운 본문으로 구성됩니다.

```yaml
---
name: skill-name
description: 스킬 설명. 언제 이 스킬을 참조해야 하는지 명시.
---

# 스킬 제목

## When to Apply
- 언제 이 스킬을 적용해야 하는지 조건 나열

## Quick Reference
- 빠른 참조를 위한 핵심 규칙 목록

## How to Use
- 상세 사용 방법 및 예제
```

### 주요 필드 설명

| 필드 | 설명 |
|------|------|
| `name` | 스킬 식별자 (폴더명과 동일 권장) |
| `description` | **트리거 조건** 포함. AI가 이 설명을 읽고 관련 작업 시 자동으로 스킬을 참조함 |

---

## 스킬 적용 예시: Vercel React Best Practices

Vercel Engineering에서 제공하는 React/Next.js 성능 최적화 가이드라인 스킬입니다.

### 스킬 구조

```
.agent/skills/
└── vercel-react-best-practices/
    ├── SKILL.md        # 스킬 진입점 (카테고리 및 빠른 참조)
    ├── AGENTS.md       # 전체 통합 문서 (모든 규칙 상세 설명)
    └── rules/          # 개별 규칙 파일 (47개)
        ├── async-parallel.md
        ├── bundle-barrel-imports.md
        └── ...
```

### 카테고리 구성 (우선순위순)

| 우선순위 | 카테고리 | 영향도 | 접두사 |
|----------|----------|--------|--------|
| 1 | 워터폴 제거 | 🔴 CRITICAL | `async-` |
| 2 | 번들 사이즈 최적화 | 🔴 CRITICAL | `bundle-` |
| 3 | 서버사이드 성능 | 🟠 HIGH | `server-` |
| 4 | 클라이언트 데이터 페칭 | 🟡 MEDIUM-HIGH | `client-` |
| 5 | 리렌더 최적화 | 🟢 MEDIUM | `rerender-` |
| 6 | 렌더링 성능 | 🟢 MEDIUM | `rendering-` |
| 7 | JavaScript 성능 | 🔵 LOW-MEDIUM | `js-` |
| 8 | 고급 패턴 | ⚪ LOW | `advanced-` |

### 트리거 조건 (description에서 발췌)

> React and Next.js performance optimization guidelines from Vercel Engineering. This skill should be used when writing, reviewing, or refactoring React/Next.js code to ensure optimal performance patterns. Triggers on tasks involving React components, Next.js pages, data fetching, bundle optimization, or performance improvements.

**즉, 다음 작업 시 자동으로 참조됩니다:**

- React 컴포넌트 작성
- Next.js 페이지 작성
- 데이터 페칭 구현
- 번들 최적화
- 성능 개선 리팩토링

---

## 스킬 설치 방법

### 1. 폴더 생성

```bash
mkdir -p .agent/skills
```

### 2. 스킬 복사

외부 스킬을 `.agent/skills/` 하위에 복사합니다.

```
.agent/skills/vercel-react-best-practices/
```

### 3. (선택) 프로젝트에 맞게 수정

- `SKILL.md`의 `description`을 프로젝트 특성에 맞게 조정
- 불필요한 규칙 제거 또는 프로젝트별 규칙 추가

---

## 커스텀 스킬 생성

프로젝트별 코딩 스타일 가이드, 아키텍처 규칙 등을 스킬로 만들 수 있습니다.

### 예시: 프로젝트 코딩 스타일 스킬

```
.agent/skills/coding-style/
├── SKILL.md
└── examples/
    ├── naming-conventions.md
    └── folder-structure.md
```

```yaml
# SKILL.md
---
name: coding-style
description: 프로젝트 코딩 스타일 가이드. 새 파일 생성, 리팩토링, 코드 리뷰 시 참조.
---

# 프로젝트 코딩 스타일 가이드

## 네이밍 규칙
- 컴포넌트: PascalCase
- 함수/변수: camelCase
- 상수: SCREAMING_SNAKE_CASE

## 폴더 구조
- `src/components/` - UI 컴포넌트
- `src/hooks/` - 커스텀 훅
- `src/utils/` - 유틸리티 함수
```

---

## Workflows vs Skills 사용 시나리오

### Workflows 사용 (명시적 작업 자동화)

- `/commit` - 변경 사항 분석 후 커밋 메시지 생성
- `/prd-prepare` - PRD inbox 파일을 정형화된 초안으로 변환

### Skills 사용 (컨텍스트 기반 지식 참조)

- React 컴포넌트 작성 시 → Vercel Best Practices 자동 참조
- 데이터베이스 스키마 설계 시 → DB 설계 가이드라인 참조
- API 엔드포인트 작성 시 → REST API 규칙 참조

---

## 참고 자료

- [SKILL-KR.md](SKILL-KR.md) - Feature Planner 스킬 예시
- [SKILL.md](SKILL.md) - 스킬 템플릿 영문 버전
- [AI워크플로우_구조설명.md](AI워크플로우_구조설명.md) - Workflows 시스템 설명
